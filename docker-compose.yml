version: '3.8'

services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./www:/usr/share/nginx/html:ro
    networks:
      - default

networks:
  default:
    external:
      name: network_services
